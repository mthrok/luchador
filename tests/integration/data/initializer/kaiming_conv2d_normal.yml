initializer:
  name: Kaiming
  args:
    uniform: False

test_config:
  shape: [64, 32, 8, 8]

compare_config:
  threshold: 0.03
  mean: 0.0
  std: 0.0221
  # fan_in = 32 * 8 * 8
  # std = np.sqrt(1. / fan_in)
